# Secrets
.env
.streamlit/secrets.toml

# Generated data / vector store
# NOTE: chroma_db/ is intentionally NOT ignored — it is committed so the
# Streamlit Cloud deploy has a prebuilt knowledge base. Re-run `python ingest.py`
# and commit the changes whenever runbooks change.

# Python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
venv/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
